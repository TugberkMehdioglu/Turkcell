@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<div class="alert alert-danger">
    @foreach (string item in Model.Errors)
    {
        <p>@item</p>
    }
</div>